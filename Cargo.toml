[workspace]
resolver = "2"
members = ["runner", "config", "scraper", "model"]

[workspace.package]
edition = "2024"

[workspace.dependencies]
anyhow = "1"

toml = "1"
serde = { version = "1", features = ["derive"] }

tracing = "0.1"

tokio = {version = "1.5", features = ["full"]}